apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../auth
  - ingress.yaml
patches:
  - target:
      group: agents.x-k8s.io
      version: v1beta1
      kind: Sandbox
      name: app
    patch: |-
      - op: replace
        path: /spec/podTemplate/spec/containers/0/image
        value: __GOBLIN_IMAGE__
      - op: replace
        path: /spec/podTemplate/spec/containers/0/imagePullPolicy
        value: Never
      - op: replace
        path: /spec/podTemplate/spec/containers/0/env/0/value
        value: http://__GOBLIN_PUBLIC_HOSTNAME__
      - op: add
        path: /spec/podTemplate/spec/containers/0/env/-
        value:
          name: GOBLIN_ALLOW_INSECURE_HTTP
          value: "true"
  - target:
      group: networking.k8s.io
      version: v1
      kind: NetworkPolicy
      name: goblin-auth
    patch: |-
      - op: add
        path: /spec/ingress/-
        value:
            from:
              - namespaceSelector:
                  matchLabels:
                    kubernetes.io/metadata.name: kube-system
                podSelector:
                  matchLabels:
                    app.kubernetes.io/name: traefik
            ports:
              - protocol: TCP
                port: 8787
# Keep execution on the deployed image by updating the existing entry by name.
replacements:
  - source:
      group: agents.x-k8s.io
      version: v1beta1
      kind: Sandbox
      name: app
      fieldPath: spec.podTemplate.spec.containers.[name=auth].image
    targets:
      - select:
          group: agents.x-k8s.io
          version: v1beta1
          kind: Sandbox
          name: app
        fieldPaths:
          - spec.podTemplate.spec.containers.[name=auth].env.[name=GOBLIN_EXECUTION_IMAGE].value
