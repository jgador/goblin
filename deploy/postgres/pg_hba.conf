# Local administration is available only to the postgres OS user in this pod.
local all goblin_admin peer map=goblin_admin
local all all reject
# PostgreSQL verifies the client chain and requires CN to equal the login role.
hostssl all goblin_app,goblin_admin all cert
host all all all reject
