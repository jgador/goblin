apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: goblin
resources:
  - victorialogs.yaml
  - fluent-bit.yaml
configMapGenerator:
  - name: goblin-fluent-bit-config
    files:
      - fluent-bit.conf
